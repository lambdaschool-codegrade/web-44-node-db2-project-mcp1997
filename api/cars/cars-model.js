const db = require('../../data/db-config')

const getAll = async () => {
  return 'get wired'
}

const getById = async (id) => {
  return 'get by id wired'
}

const create = async (car) => {
  return 'create wired'
}

module.exports = {
  getAll,
  getById,
  create
}
